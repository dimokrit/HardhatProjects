export const config = {
  // main
  //apiGraphql: process.env.NODE_ENV === "development" ? 'http://localhost:5000/api/graphql' : '/api/graphql',
  //apiExpress: process.env.NODE_ENV === "development" ? 'http://localhost:5000/api/' : '/api/',
  apiGraphql: '/api/graphql',
  apiExpress: '/api/',
}
